# Migration `20200716190825-init`

This migration has been generated by Alexandre at 7/16/2020, 7:08:25 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"email" text  NOT NULL ,
"firstname" text  NOT NULL ,
"id" SERIAL,
"lastname" text  NOT NULL ,
"phone" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Cause" (
"description" text   ,
"id" SERIAL,
"userId" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."SocialNetwork" (
"facebook" text   ,
"id" SERIAL,
"instagram" text   ,
"linkedin" text   ,
"twitter" text   ,
"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "SocialNetwork.userId" ON "public"."SocialNetwork"("userId")

ALTER TABLE "public"."Cause" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."SocialNetwork" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200716190825-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource postgresql {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id Int @default(autoincrement()) @id
+  firstname String 
+  lastname String
+  email String   @unique
+  phone String?
+  interests Cause[]
+  socialNetwork SocialNetwork? 
+}
+
+model Cause{
+  id Int @default(autoincrement()) @id
+  description String?
+}
+
+model SocialNetwork{
+  id Int @default(autoincrement()) @id
+  facebook String?
+  instagram String?
+  twitter String?
+  linkedin String?
+  user User     @relation(fields: [userId], references: [id])
+  userId Int    @unique
+}
```


